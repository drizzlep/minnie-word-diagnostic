{
  "name": "minnie-word-diagnostic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.js",
    "test": "node --test",
    "validate": "npm test && node scripts/validate-data.js"
  }
}
